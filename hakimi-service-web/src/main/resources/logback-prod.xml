<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 1. 定义属性变量，方便统一管理 -->
    <property name="LOG_PATH" value="/data/logs"/>
    <property name="LOG_FILE_NAME" value="hakimi-app-conversation"/>
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>

    <!-- 2. 定义控制台输出 Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <!-- 可选：设置控制台只输出INFO及以上级别 -->
        <!--         <filter class="ch.qos.logback.classic.filter.ThresholdFilter"> -->
        <!--             <level>INFO</level> -->
        <!--         </filter> -->
    </appender>

    <!-- 3. 定义文件输出 Appender (支持滚动) -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${LOG_FILE_NAME}.log</file>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <!-- 滚动策略：按时间和大小滚动 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- 日志文件归档的命名模式 -->
            <fileNamePattern>${LOG_PATH}/${LOG_FILE_NAME}.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <!-- 单个日志文件最大大小 -->
            <maxFileSize>10MB</maxFileSize>
            <!-- 日志文件保留的最长时间，单位天 -->
            <maxHistory>30</maxHistory>
            <!-- 所有归档日志文件的总大小上限 -->
            <totalSizeCap>5GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- 4. 配置根日志记录器 -->
    <root level="INFO">
        <!-- 引用上面定义的两个Appender -->
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- 5. （可选）为特定包设置更细粒度的日志级别 -->
    <!-- 例如，让 com.example 包下的日志输出DEBUG级别，方便调试 -->
    <!--    <logger name="com.example" level="DEBUG" additivity="false">-->
    <!--        &lt;!&ndash; additivity="false" 表示这条日志不会重复传递给root，这里我们手动指定它也输出到控制台和文件 &ndash;&gt;-->
    <!--        <appender-ref ref="CONSOLE"/>-->
    <!--        <appender-ref ref="FILE"/>-->
    <!--    </logger>-->
    <!-- 例如，减少第三方库的日志噪音，只输出WARN及以上级别 -->
    <!--    <logger name="org.springframework.web" level="WARN"/>-->

</configuration>